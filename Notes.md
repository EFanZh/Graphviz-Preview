# Notes

## Publish workflow

- Update version number.
- Run `npm update`.
- Update change log and tag links.
- Push to master.
- Run `vsce publish`.
- Add release tag.

## TODO

- [ ] Allow user to configure the default scale mode.
